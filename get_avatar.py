import streamlit as st

def get_avatar():
    st.title("Guide to Generate the avatar")

    # Display a header
    st.header("We are using Kaggle for now...")

    # Display a message with a clickable link
    st.write("""
        Instructions will be written here.
        Later this will be automated to a api call. 
        Now it is done manually using a kaggle notebook.
    """)
